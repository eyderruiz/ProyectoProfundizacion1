<div class="page-header">
<h2 class="@(Centrado ? "center" : "")">
    @Texto
</h2>
</div>

@code {
    [Parameter] public string Texto { get; set; } = "";
    [Parameter] public bool Centrado { get; set; } = false;
}